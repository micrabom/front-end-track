import React from 'react';
import ReactDOM from 'react-dom';
import Bootstrap from './components/Blog';



const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
	<React.StrictMode>
		<Bootstrap />
	</React.StrictMode>
);
